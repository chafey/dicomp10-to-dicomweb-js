const dicomp10todicomweb = async (dicomp10, callback, options) => {

    // Read dicomp10 stream into buffer

    // Parse it

    // Iterate over parsed data and
        // generate metadata
        // generate bulk items
        // generate image frames

    // resolve promise with statistics
}

module.exports = dicomp10todicomweb